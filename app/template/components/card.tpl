
<div class="card mt-2 mb-2">
    {{card-header}}
    {{card-img}}
    <div class="card-body">
        {{card-title}}
        {{card-subtitle}}
        {{card-text}}
        {{card-button}}
        {{others}}
    </div>
</div>
